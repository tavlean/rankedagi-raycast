# CLAUDE.md

Read .claude/PROJECT_BRIEF.md for project goals and current state, and docs/fable-plans/ROADMAP.md for the plan + status (single source of truth).

This is a Raycast extension (TypeScript + React, npm package at repo root). Verify changes with `npm run build` and `npm run lint`. When touching Raycast API surface you haven't used this session, check current docs via the raycast-api MCP server first — the report in docs/research/ was current on 2026-07-02 but the API moves fast.
